vagrant box add --name "name" "path" :adds the box to vagrant

vagrant up :runs the box

vagrant ssh :grants you acces to the box and allowes you to work with commands on that box

vagrant exit :exit the box

vagrant box list :show list of boxes

vagrant box remove :remove a certain box

vagrant ssh "srvname": open a certain running box
